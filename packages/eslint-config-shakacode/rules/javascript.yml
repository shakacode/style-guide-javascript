---
rules:

  # http://eslint.org/docs/rules/no-return-assign.html
  no-return-assign:
    - 2
    - "always"

  # http://eslint.org/docs/rules/no-param-reassign.html
  no-param-reassign:
    - 2
    - props: false

  no-unused-vars:
    - 2
    - argsIgnorePattern: ^_
